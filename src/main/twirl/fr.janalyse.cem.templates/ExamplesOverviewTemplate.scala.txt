@(context:OverviewContext)
# @context.title
- Generated by [@context.projectName](@context.projectURL) release @context.version
- @context.examplesCount published examples

@for(exs4cat <- context.examplesByCategory) {## @exs4cat.category
@for(ex <- exs4cat.categoryExamples)        {- [@ex.filename](@ex.url) : @ex.summary
}
}
